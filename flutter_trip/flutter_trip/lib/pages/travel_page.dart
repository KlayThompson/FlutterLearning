import 'package:flutter/material.dart';

class TravelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('旅拍'),
        ),
        body: Container(child: Center(child: Text('旅拍'),),)
    );;
  }
}
